import { SupportTicketsHeader } from "@/components/super admin section/support tickets/supportticketsheader";
import { Box } from "@/components/ui/box";

const SuperAdminSupportTicketPage = () => {
  return (
    <Box className="px-2">
      <SupportTicketsHeader />
    </Box>
  );
};

export default SuperAdminSupportTicketPage;

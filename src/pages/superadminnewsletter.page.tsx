import { NewsletterHeader } from "@/components/super admin section/newsletter/newsletterheader";
import { Box } from "@/components/ui/box";

export const SuperAdminNewsletterPage = () => {
  return (
    <Box className="px-2">
      <NewsletterHeader />
    </Box>
  );
};

